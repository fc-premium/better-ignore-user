{
	"force-update": {
		"type": "boolean",
		"title": "Force update",
		"description": "Always check for new ignored users",
		"default": false
	},

	"hide-posts": {
		"type": "boolean",
		"title": "Hide posts",
		"description": "Hides the posts instead of showing a message",
		"default": false
	}
}
